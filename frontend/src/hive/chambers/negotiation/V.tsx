/**
 * Re-exporting Visual Nucleotide (V) from the Negotiation Chamber.
 * This pattern ensures the chamber source remains the source of truth
 * while allowing the frontend build system to correctly resolve it.
 */
export * from '../../../../../core/src/hive/chambers/negotiation/V';
