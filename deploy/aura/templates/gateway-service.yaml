apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-gateway"
spec:
  type: {{ .Values.gateway.service.type }}
  selector:
    app: gateway
  ports:
    - port: {{ .Values.gateway.service.port }}
      targetPort: 8000
      protocol: TCP